#include<stdio.h>
struct compno{
    int real, imag;
}a,b,c;

int main(){
    scanf("%d %d %d",&a.real, &b.real, &c.real);
    scanf("%d %d %d",&a.imag, &b.imag, &c.imag);
   printf("a*i%b,b*i%c,c*i%a");

    return 0;
}
